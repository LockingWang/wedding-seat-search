# API mode:
# - auto: 先打 API，失敗回退 mock（預設）
# - api: 只打 API，不做 mock fallback
# - mock: 只使用本地 mock
VITE_API_MODE=auto

# Backend API base URL (自製 server / EC2 網址)
# 例如: https://api.your-domain.com
VITE_API_BASE_URL=
