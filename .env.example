# API mode:
# - auto: 先打 API，失敗回退 mock（預設）
# - api: 只打 API，不做 mock fallback
# - mock: 只使用本地 mock
VITE_API_MODE=auto

# Google Apps Script Web App base URL (api mode / auto mode 會使用)
# 例如: https://script.google.com/macros/s/xxxxxxxxxxxx/exec
VITE_APPS_SCRIPT_BASE_URL=
